const BASE_URL = "https://api.spotify.com/v1";

export const albumsConstant = {
  GET_NEW_RELEASES_URL: `${BASE_URL}/browse/new-releases`,
  SEARCH_ALBUMS_URL: `${BASE_URL}/search`,
};

export const usersConstant = {
  GET_USER_PROFILE: `${BASE_URL}/me`,
};